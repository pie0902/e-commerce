# Cloudflare Tunnel config example.
# Copy to config.yml and edit hostnames for your domain.
# Keep credentials JSON out of git.

tunnel: thunderdev-site
credentials-file: /etc/cloudflared/thunderdev-site.json

ingress:
  - hostname: thunderdev.site
    service: http://localhost:8080
  - service: http_status:404
